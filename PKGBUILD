# Maintainer: Gauthier Voron <gauthier.voron@lip6.fr>
pkgname=grub-list
pkgver=1.0.0
pkgrel=1
epoch=1
pkgdesc="List the entries of the grub menu"
arch=('any')
url=""
license=('GPL')
groups=()
depends=('perl')
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=("grub-list")
noextract=()
md5sums=('SKIP')
validpgpkeys=()

package() {
    mkdir -p "$pkgdir/usr/bin/"
    cp "grub-list" "$pkgdir/usr/bin/grub-list"
}
